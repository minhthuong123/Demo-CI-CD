name: CI Pipeline

# Sự kiện trigger pipeline
on:
  workflow_dispatch:  # Cho phép trigger thủ công

jobs:
  Smoke:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js 18.12.1
        uses: actions/setup-node@v3
        with:
          node-version: '18.12.1'

      - name: Install dependencies
        run: npm install

      - name: Run test case
        run: npm run f L-01

  # deploy_report:
  #   runs-on: macos-latest
  #   needs: Smoke  # Chỉ chạy sau khi Smoke thành công

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v3

  #     - name: Set up Node.js 18.12.1
  #       uses: actions/setup-node@v3
  #       with:
  #         node-version: '18.12.1'

  #     - name: Install dependencies
  #       run: npm install

  #     - name: Deploy report
  #       run: npm run deploy

  send_notifications_to_lark:
    runs-on: macos-latest
    needs: [Smoke]  # Chỉ chạy sau khi Smoke và deploy_report thành công

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Node.js 18.12.1
        uses: actions/setup-node@v3
        with:
          node-version: '18.12.1'

      - name: Install dependencies
        run: npm install

      - name: Send notifications
        run: |
          echo "Send notifications................................................................"
          node src/core/messageToLark.js